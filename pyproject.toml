[project]
name = "3sixd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "opencv",
    "torch>=2.0.1",
    "torchvision>=0.15.2",
]

[tool.uv.sources]
opencv = { path = "../opencv/build/python_loader" }
